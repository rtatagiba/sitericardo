{
  "name": "@sitericardo/ax-core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Analisador compartilhado da árvore de acessibilidade — sem dependência de runtime, consumido pelo Worker e por qualquer skill local via node.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
