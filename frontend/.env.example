# URL do Cloudflare Worker que faz a ponte com a API do GitHub (painel /admin)
# Veja cloudflare-worker/README.md para o passo a passo do deploy
PUBLIC_ADMIN_WORKER_URL=https://blog-admin.SEU-SUBDOMINIO.workers.dev
